<!DOCTYPE html>
<html lang="en">

<head>
    <%- include('./partials/head') %>
        <title>
            <%= title %>
        </title>
</head>

<body>
    <header><%- include('./partials/header') %></header>
    <main>
        <h1>
            <%= title %>
        </h1>

        <ul>
            <% for(let i=0;i < genus_list.length; i++) { %>
                <li>
                    <a href="<%= genus_list[i].url %>">
                        <%= genus_list[i].name %>
                    </a>
                    of the
                    <a href="<%= genus_list[i].family.url %>">
                        <%= genus_list[i].family.name + " family" %>
                    </a>
                </li>
                <% } %>
        </ul>
    </main>
</body>

</html>